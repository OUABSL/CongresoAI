const AuthContext = {
    sessionToken: null,
    role: null,
    username: null,
    setSessionToken: (token) => {},
    setRole: (role) => {},
    setUsername: (username) => {},
  };
  
  export default AuthContext;